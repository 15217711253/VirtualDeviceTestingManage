<Window x:Class="VirtualDeviceManage.App.Views.ArtechDeviceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VirtualDeviceManage.App.Views"
        mc:Ignorable="d"
        Title="ArtechDeviceView" Height="450" Width="800">
    <StackPanel Orientation="Vertical">
        <Label Content="{Binding Name}" Foreground="Black" FontSize="30"/>
        <CheckBox Content="当前电源状态" IsChecked="{Binding Power,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
        <StackPanel Orientation="Horizontal">
            <Label Content="当前表演节目" Foreground="Black" FontSize="30"/>
            <TextBox Text="{Binding ShowId,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button Content="自加" Margin="6" Command="{Binding ChangeLampTimeCommand}" CommandParameter="Add" />
            <Button Content="自减" Margin="6" Command="{Binding ChangeLampTimeCommand}" CommandParameter="Minus"/>
        </StackPanel>
        <local:DebugTestView Width="350" x:Name="DeBugUserControl"/>
    </StackPanel>
</Window>
